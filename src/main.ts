import "./style.css";

type Topic = "dbms" | "java" | "python";
type View = "dashboard" | "learn" | "progress" | "history" | "summary" | "plan" | "profile" | "subjects" | "classroom";
type Q = { type:string; question:string; hint:string; focus:string; concept?:string; difficulty?:number; reason?:string };
type D = { is_correct:boolean; concept:string; concept_label:string; misconception:boolean; misconception_id:string|null; confidence_assessment:string; summary:string; explanation:string; recovery:string; next_question:string; mastery_signal:number; mode?:string; old_mastery?:number; new_mastery?:number; adaptive_next?:Q };
type SessionSignal={correct:boolean;confidence:string;misconception:boolean;concept:string;label:string};

type State={authenticated:boolean;authMode:"login"|"signup";authName:string;authEmail:string;authPassword:string;authError:string;modal:View|null;view:View;topic:Topic;q:Q|null;answer:string;confidence:string;d:D|null;mastery:Record<string,number>;attempts:number;name:string;grade:string;goal:string;preferred_input:string;history:any[];loading:boolean;booting:boolean;map:any[];session:SessionSignal[];sessionStarted:number;sessionTarget:number;demoMode:boolean;toast:string};
const s:State={authenticated:false,authMode:"login",authName:"",authEmail:"",authPassword:"",authError:"",modal:null,view:"dashboard",topic:"dbms",q:null,answer:"",confidence:"",d:null,mastery:{},attempts:0,name:"Student",grade:"College",goal:"Build strong understanding",preferred_input:"Text",history:[],loading:false,booting:true,map:[],session:[],sessionStarted:0,sessionTarget:3,demoMode:false,toast:""};
const app=document.querySelector("#app")!;
const API=(import.meta.env.VITE_API_URL || (window.location.hostname === "localhost" ? "http://localhost:8787" : window.location.origin)).replace(/\/$/, "");
const readJson = async (response: Response) => {
  const text = await response.text();
  if (!text) return null;
  try { return JSON.parse(text); }
  catch {
    throw new Error(`Server returned invalid JSON (${response.status}). Please check that the backend is running.`);
  }
};
const apiFetch=(url:string,options:RequestInit={})=>{const headers=new Headers(options.headers||{});const token=localStorage.getItem("learnback_session");if(token)headers.set("X-Session-Token",token);const requestUrl=url.startsWith("http")?url:`${API}${url}`;return fetch(requestUrl,{...options,headers})};
const conceptLabel=(id:string)=>({primary_key:"Primary Key",foreign_key:"Foreign Key",candidate_key:"Candidate Key",normalization:"Normalization",encapsulation:"Encapsulation",inheritance:"Inheritance",polymorphism:"Polymorphism",lists:"Lists",functions:"Functions",oop_python:"OOP in Python",exceptions:"Exceptions"}[id]||id.replace(/_/g," "));
const pct=(v:number)=>Math.round(Number(v||0));
function authScreen(){return `<div class="auth-shell"><div class="auth-brand"><b class="logo">L</b><div><strong>LearnBack</strong><small>adaptive learning</small></div></div><div class="auth-card"><div class="auth-kicker">YOUR LEARNING SPACE</div><h1>${s.authMode==="login"?"Welcome back.":"Create your learning profile."}</h1><p>${s.authMode==="login"?"Sign in to continue with your personal knowledge map and learning history.":"Start a personal learning journey. Your progress stays linked to your account."}</p>${s.authError?`<div class="auth-error">${s.authError}</div>`:""}${s.authMode==="signup"?`<label class="auth-field"><span>Your name</span><input id="auth-name" placeholder="e.g. Rahul" value="${s.authName}"></label>`:""}<label class="auth-field"><span>Email</span><input id="auth-email" type="email" placeholder="you@example.com" value="${s.authEmail}"></label><label class="auth-field"><span>Password</span><input id="auth-password" type="password" placeholder="${s.authMode==="signup"?"At least 6 characters":"Your password"}" value="${s.authPassword}"></label>${s.authMode==="signup"?`<div class="auth-note">Your account unlocks a separate profile, mastery map and history.</div>`:""}<button class="primary auth-submit" id="auth-submit">${s.authMode==="login"?"Log in →":"Create account →"}</button><div class="auth-switch">${s.authMode==="login"?`New to LearnBack? <button data-auth-switch="signup">Create an account</button>`:`Already have an account? <button data-auth-switch="login">Log in</button>`}</div></div><div class="auth-foot">LearnBack • Understand. Adapt. Improve.</div></div>`}
async function authSubmit(){s.authError="";const email=(document.querySelector<HTMLInputElement>("#auth-email")?.value||"").trim();const password=document.querySelector<HTMLInputElement>("#auth-password")?.value||"";const name=document.querySelector<HTMLInputElement>("#auth-name")?.value.trim()||"";s.authEmail=email;s.authPassword=password;s.authName=name;if(s.authMode==="signup"&&name.length<2){s.authError="Please enter your name.";render();return}if(!/^\S+@\S+\.\S+$/.test(email)){s.authError="Please enter a valid email.";render();return}if(password.length<6){s.authError="Password must be at least 6 characters.";render();return}try{const r=await fetch(`${API}/api/${s.authMode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(s.authMode==="signup"?{name,email,password}:{email,password})});const data=await readJson(r);if(!r.ok)throw new Error((data && data.error) || "Authentication failed");localStorage.setItem("learnback_session",data.token);s.authenticated=true;s.authError="";s.authPassword="";await boot(true)}catch(e:any){s.authError=e.message||"Could not sign in.";render()}}
function header(label:string,title:string,action:string,v:View,target?:View){const dest=target||v;const attr=dest==="learn"?`data-v="${dest}"`:`data-modal="${dest}"`;return `<header><div><small>${label}</small><h1>${title}</h1></div><button class="ghost" ${attr}>${action}</button></header>`}
function row(name:string,v:number,id=""){const status=v<60?"Needs attention":v<80?"Developing":"Strong";return `<div class="row" data-concept="${id}"><i></i><div><b>${name}</b><span>${status}</span></div><em><u style="width:${Math.max(0,Math.min(100,v))}%"></u></em><strong>${pct(v)}%</strong></div>`}
function side(){return `<aside><div class="brand" data-modal="dashboard"><b class="logo">L</b><div><strong>LearnBack</strong><small>adaptive learning</small></div></div><nav><button data-modal="dashboard">⌂ <span>Overview</span></button><button data-v="learn">✦ <span>Learn</span></button><button data-modal="progress">◔ <span>Knowledge map</span></button><button data-modal="classroom">◎ <span>Classroom copilot</span></button><button data-modal="subjects">＋ <span>Subjects</span></button><button data-modal="plan">✦ <span>Learning plan</span></button><button data-modal="history">↺ <span>History</span></button><button data-modal="profile">◉ <span>My profile</span></button></nav><div class="bottom"><div class="signal"><small>TODAY</small><b>${s.attempts} learning signals</b><span>Stored in your local database.</span></div><div class="user"><i>${s.name[0]?.toUpperCase()||"S"}</i><div><b>${s.name}</b><small>${s.grade}</small></div></div></div></aside>`}
function dashboard(){const vals=Object.values(s.mastery);const overall=vals.length?vals.reduce((a,b)=>a+Number(b),0)/vals.length:62;const weak=Object.entries(s.mastery).sort((a,b)=>a[1]-b[1]).slice(0,3);const high=s.history.filter(x=>x.misconception&&String(x.confidence).toLowerCase().includes("confident")).length;const correct=s.history.filter(x=>x.is_correct).length;const insight=high?`You have ${high} high-confidence error${high>1?"s":""} to revisit. LearnBack will target them instead of restarting the whole topic.`:"Confidence is a learning signal. Answer naturally, then tell LearnBack how sure you felt.";return `${header("LEARNING OVERVIEW",`Good to see you, ${s.name}.`,"Knowledge map","progress")}<section class="hero"><div><small>LEARNBACK RECOMMENDS</small><h2>${high?"Let's repair a misconception.":"Let's find what you need next."}</h2><p>LearnBack uses your answer, confidence, history and concept mastery to decide the next learning move.</p><div class="hero-actions"><button class="primary" data-start="dbms">Start adaptive session →</button><button class="demo-btn" data-demo="1">▶ Run 60-sec demo</button><button class="hero-link" data-start="java">Try Java OOP</button><button class="hero-link" data-modal="subjects">Browse subjects</button><button class="hero-link" data-v="plan">View today's plan</button></div></div><div class="orb"><span>${pct(overall)}%</span><small>profile</small></div></section><div class="stats"><div><small>LEARNING SIGNALS</small><b>${s.attempts}</b><span>Saved locally</span></div><div><small>CONCEPTS TRACKED</small><b>${Object.keys(s.mastery).length||7}</b><span>Across your profile</span></div><div><small>UNDERSTANDING</small><b>${pct(overall)}%</b><span>Current estimate</span></div><div><small>HIGH-CONFIDENCE ERRORS</small><b>${high}</b><span>Being targeted</span></div></div><div class="grid"><section class="panel"><div class="head"><div><small>KNOWLEDGE MAP</small><h3>Where you stand</h3></div><button class="link" data-v="progress">Open map</button></div>${weak.length?weak.map(x=>row(conceptLabel(x[0]),x[1],x[0])).join(""):row("Primary Key",42,"primary_key")+row("Foreign Key",68,"foreign_key")+row("Polymorphism",51,"polymorphism")}</section><section class="panel insight"><small>LEARNER INSIGHT</small><div class="star">✦</div><h3>${high?"Your confidence pattern matters.":"Confidence is a learning signal."}</h3><p>${insight}</p><div class="insight-pill">Answer → Diagnose → Adapt → Re-test</div></section></div><div class="section"><small>CONTINUE LEARNING</small><h3>Choose a learning track</h3></div><div class="topics"><button data-start="dbms"><i>DB</i><div><b>Database Keys</b><span>DBMS • misconception recovery</span></div><strong>${pct(s.mastery.primary_key??42)}%</strong></button><button data-start="java"><i>JV</i><div><b>OOP Basics</b><span>Java • concept progression</span></div><strong>${pct(s.mastery.encapsulation??74)}%</strong></button><button data-start="python"><i>PY</i><div><b>Python Foundations</b><span>Python • adaptive concept recovery</span></div><strong>${pct(s.mastery.lists??50)}%</strong></button></div>`}
function learn(){if(s.d)return diagnosis();const q=s.q;if(!q)return `<section class="loading-card panel"><div class="spinner"></div><h2>Building your next question…</h2><p>LearnBack is checking your current knowledge state.</p></section>`;const canVoice="SpeechRecognition" in window||"webkitSpeechRecognition" in window;return `${header(s.topic==="dbms"?"DBMS • DATABASE KEYS":s.topic==="java"?"JAVA • OOP BASICS":"PYTHON • FOUNDATIONS","Adaptive learning session","Exit","dashboard")}<div class="session-top"><span>Session ${s.session.length+1} of ${s.sessionTarget}</span><em><u style="width:${Math.min(100,(s.session.length/s.sessionTarget)*100)}%"></u></em>${s.demoMode?`<span class="demo-chip">DEMO STORY</span>`:""}<button class="mini" data-v="summary">End session</button></div><div class="session"><section class="panel question"><div class="step"><span>Adaptive question</span><em><u style="width:${Math.min(92,25+s.attempts%4*18)}%"></u></em><span>${q.difficulty?`Level ${q.difficulty}`:"Personalized"}</span></div><div class="question-meta"><small class="tag">${q.type}</small><small class="reason">${q.reason||"Selected from your knowledge state"}</small></div><h2>${q.question}</h2><p>${q.hint}</p><div class="answer-wrap"><textarea id="answer" placeholder="Write your answer naturally...">${s.answer}</textarea>${canVoice?`<button class="voice" id="voice">🎙 Voice answer</button>`:`<small class="voice-note">Voice input is not supported by this browser.</small>`}</div>${s.demoMode?`<div class="demo-strip"><div><b>Hackathon demo</b><span>Show a high-confidence misconception in one click.</span></div><button class="demo-fill" data-fill-demo>Fill sample answer</button></div>`:""}<div class="confidence"><div><b>How confident are you?</b><span>This lets LearnBack distinguish uncertainty from confident misconceptions.</span></div><div>${["Not sure","Somewhat","Confident","Very confident"].map(x=>`<button data-c="${x}" class="${s.confidence===x?"selected":""}">${x}</button>`).join("")}</div></div><div class="actions"><button class="primary" id="submit" ${!s.answer.trim()||!s.confidence||s.loading?"disabled":""}>${s.loading?"Analyzing learning signal…":"Analyze my answer →"}</button></div></section><aside class="side"><div class="sidecard focus-card"><small>CURRENT FOCUS</small><div class="focus-icon">◎</div><h3>${q.focus}</h3><p>Target selected from the learner's current knowledge state, not a fixed quiz sequence.</p></div><div class="sidecard"><small>LEARNBACK LOOP</small><div class="mini-flow"><span>Answer</span><b>→</b><span>Diagnose</span><b>→</b><span>Adapt</span></div><p>Every response becomes a learning signal.</p></div><div class="sidecard signal-card"><small>SESSION SIGNALS</small><b>${s.session.length}</b><span>answers captured</span><div class="signal-dots">${Array.from({length:s.sessionTarget},(_,i)=>`<i class="${i<s.session.length?(s.session[i]?.correct?"ok":"bad"):""}"></i>`).join("")}</div></div></aside></div>`}
function diagnosis(){const d=s.d!;return `${header("LEARNBACK DIAGNOSIS",d.misconception?"A misconception needs repair.":"Understanding signal detected.",s.session.length>=s.sessionTarget?"See session report":"Continue","learn")}<section class="result ${d.is_correct?"good":"attention"}"><i>${d.is_correct?"✓":"!"}</i><div><small>${d.is_correct?"UNDERSTANDING CONFIRMED":"MISCONCEPTION / GAP DETECTED"} • ${d.mode==="ai"?"AI":"LOCAL ENGINE"}</small><h2>${d.summary}</h2><p>${d.explanation}</p></div></section><div class="diagnosis-pipeline"><div class="done"><span>1</span><b>Answer captured</b><small>Natural response + confidence</small></div><div class="done"><span>2</span><b>Signal diagnosed</b><small>${d.misconception?"Misconception detected":"Understanding checked"}</small></div><div class="next-step"><span>3</span><b>Next move</b><small>Targeted ${s.session.length>=s.sessionTarget?"session report":"re-test"}</small></div></div><div class="diagnosis-grid"><section class="panel"><small>WHAT LEARNBACK SAW</small><h3>${d.concept_label}</h3><div class="signals"><div><span>Correctness</span><b>${d.is_correct?"Correct":"Needs work"}</b></div><div><span>Confidence assessment</span><b>${d.confidence_assessment}</b></div><div><span>Misconception</span><b>${d.misconception?d.misconception_id||"Detected":"Not detected"}</b></div></div><div class="box"><b>Recovery explanation</b><p>${d.recovery}</p></div></section><section class="panel recovery"><small>NEXT BEST ACTION</small><h3>Target the exact concept.</h3><div class="next"><i>1</i><div><b>Recovery</b><p>${d.recovery}</p></div></div><div class="next"><i>2</i><div><b>Targeted question</b><p>${d.next_question}</p></div></div><button class="primary" id="continue">${s.session.length>=s.sessionTarget?"View session report →":"Continue adaptive session →"}</button></section></div><div class="update"><div><span>${d.concept_label}</span><small>mastery update</small></div><b>${pct(d.old_mastery??50)}% → ${pct(d.new_mastery??50)}</b></div>`}
function summary(){const total=s.session.length,correct=s.session.filter(x=>x.correct).length,mis=s.session.filter(x=>x.misconception).length,high=s.session.filter(x=>x.misconception&&/confident/i.test(x.confidence)).length,acc=total?Math.round(correct/total*100):0;const duration=s.sessionStarted?Math.max(1,Math.round((Date.now()-s.sessionStarted)/60000)):0;const concepts=[...new Set(s.session.map(x=>x.label))];return `${header("SESSION REPORT","Your learning session","Back to overview","dashboard")}<section class="summary-hero"><div><small>LEARNING RECOVERY SESSION</small><h2>Here's what changed.</h2><p>LearnBack measured evidence across your answers instead of treating the session as a simple score.</p></div><div class="score-ring"><b>${acc}%</b><span>answer accuracy</span></div></section><div class="summary-stats"><div><small>ANSWERS</small><b>${total}</b></div><div><small>CONCEPTS TOUCHED</small><b>${concepts.length}</b></div><div><small>MISCONCEPTIONS</small><b>${mis}</b></div><div><small>HIGH-CONFIDENCE ERRORS</small><b>${high}</b></div></div><div class="grid"><section class="panel"><small>SESSION EVIDENCE</small><h3>What LearnBack observed</h3><div class="evidence">${s.session.map((x,i)=>`<div><i class="${x.correct?"ok":"bad"}">${x.correct?"✓":"!"}</i><span><b>${x.label}</b><small>${x.confidence} confidence • ${x.correct?"correct":"needs work"}${x.misconception?" • misconception signal":""}</small></span><strong>${i+1}</strong></div>`).join("")}</div></section><section class="panel"><small>LEARNER SIGNAL</small><h3>${high?"Confidence calibration needs attention.":mis?"A misconception was isolated.":"Your understanding signal is improving."}</h3><p>${high?"You were confident on an incorrect concept signal. LearnBack can revisit that concept with a targeted example rather than repeating the entire topic.":mis?"The session found a specific concept-level issue and used it to select a recovery path.":"Your answers gave LearnBack enough evidence to update your concept mastery."}</p><div class="summary-pill">Session time: ${duration} min • ${total} learning signals</div><button class="primary" data-start="${s.topic}">Run another adaptive session →</button></section></div>`}
function progress(){const vals=Object.values(s.mastery);const overall=vals.length?vals.reduce((a,b)=>a+Number(b),0)/vals.length:62;const db=["primary_key","foreign_key","candidate_key","normalization"],java=["encapsulation","inheritance","polymorphism"],python=["lists","functions","oop_python","exceptions"];const node=(id:string,i:number,group:string)=>{const v=s.mastery[id]??50;return `<button class="map-node ${v<60?"weak":v>=80?"strong":"developing"}" style="--x:${[10,35,60,85,30,55,80][i]}%;--y:${group==="dbms"?55:20}%" data-start="${group}"><span>${pct(v)}%</span><b>${conceptLabel(id)}</b><small>${v<60?"Needs attention":v<80?"Developing":"Strong"}</small></button>`};return `${header("LEARNING PROFILE","Your knowledge map","History","progress","history")}<section class="progresshero"><div><small>OVERALL UNDERSTANDING</small><b>${pct(overall)}%</b><p>Mastery is updated from observed evidence. The map shows relationships between concepts and where the next intervention can happen.</p></div><div class="ring"><span>${pct(overall)}%</span></div></section><section class="panel map-panel"><div class="head"><div><small>CONCEPT GRAPH</small><h3>How ideas connect</h3></div><span class="map-legend"><i></i> strong <i></i> developing <i></i> needs attention</span></div><div class="graph"><div class="graph-line l1"></div><div class="graph-line l2"></div><div class="graph-line l3"></div>${db.map((id,i)=>node(id,i,"dbms")).join("")}${java.map((id,i)=>node(id,i,"java")).join("")}${python.map((id,i)=>node(id,i,"python")).join("")}<div class="graph-label db">DBMS</div><div class="graph-label jv">JAVA</div><div class="graph-label py">PYTHON</div></div></section><div class="grid"><section class="panel"><small>CONCEPT MASTERY</small><h3>Tracked concepts</h3>${[...db,...java,...python].map(id=>row(conceptLabel(id),s.mastery[id]??50,id)).join("")}</section><section class="panel"><small>ADAPTIVE RULE</small><h3>Why the next question changes</h3><p class="rule-copy">LearnBack prioritizes weaker concepts, then uses prerequisites and recent misconception signals to decide what to revisit.</p><div class="rule-card"><span>1</span><b>Find weak concept</b><small>lowest mastery / recent error</small></div><div class="rule-card"><span>2</span><b>Repair misconception</b><small>if confidence + answer pattern support it</small></div><div class="rule-card"><span>3</span><b>Test transfer</b><small>check whether understanding generalizes</small></div></section></div>`}
function plan(){
  const entries=Object.entries(s.mastery).sort((a,b)=>a[1]-b[1]);
  const weak=entries[0]||["primary_key",42];
  const recentMis=s.history.find(x=>x.misconception);
  const recentCorrect=s.history.filter(x=>x.is_correct).length;
  const goal=Math.min(5,Math.max(3,s.sessionTarget));
  const progress=Math.min(100,Math.round((s.session.length/goal)*100));
  const actions=[
    {n:"01",tag:"REPAIR",title:`Repair ${conceptLabel(recentMis?.concept||weak[0])}`,text:recentMis?`Revisit ${recentMis.misconception_id} with a targeted example.`:`Start with your lowest-mastery concept and build a clean mental model.`,topic:recentMis?.topic||s.topic},
    {n:"02",tag:"REVIEW",title:`Strengthen ${conceptLabel(weak[0])}`,text:`Use a focused concept check, then retest without repeating the full topic.`,topic:s.topic},
    {n:"03",tag:"TRANSFER",title:"Prove it in a new context",text:"Apply the idea to a slightly harder scenario so LearnBack can test whether understanding generalizes.",topic:s.topic}
  ];
  return `${header("PERSONAL LEARNING PLAN","Your next learning moves","Start adaptive session","plan","learn")}
  <section class="plan-hero"><div><small>ADAPTIVE STUDY PLAN</small><h2>A short path built from your evidence.</h2><p>LearnBack turns your recent answers into a focused sequence: repair what is weak, review what is fading, then test transfer.</p></div><div class="goal-ring"><b>${progress}%</b><span>today's goal</span></div></section>
  <div class="plan-goal"><div><small>TODAY'S GOAL</small><b>${Math.min(s.session.length,goal)} / ${goal} learning signals</b><span>${recentCorrect?`${recentCorrect} correct signals in your history.`:"Start your first session to create evidence."}</span></div><button class="primary" data-start="${s.topic}">Start plan →</button></div>
  <div class="plan-grid">${actions.map(a=>`<article class="plan-card"><span class="plan-num">${a.n}</span><small>${a.tag}</small><h3>${a.title}</h3><p>${a.text}</p><button class="link" data-start="${a.topic}">Open this path →</button></article>`).join("")}</div>
  <section class="panel trend"><div class="head"><div><small>LEARNING TREND</small><h3>Recent evidence</h3></div><span class="trend-note">${s.history.length} signals stored</span></div><div class="trend-bars">${s.history.slice(0,12).reverse().map(x=>`<div title="${x.is_correct?"Correct":"Needs work"}"><i style="height:${x.is_correct?82:42}%"></i></div>`).join("")||'<div class="trend-empty">Complete a session to see your learning trend.</div>'}</div></section>`;
}


function profile(){return `${header("LEARNER PROFILE","Make LearnBack fit you","Close","profile","dashboard")}<section class="profile-hero"><div><small>PERSONALIZED LEARNING</small><h2>Tell LearnBack who is learning.</h2><p>Your profile shapes the learning plan, while your answers remain the strongest evidence of what you understand.</p></div><div class="profile-avatar">${s.name[0]?.toUpperCase()||"S"}</div></section><section class="panel profile-form"><div class="field"><label>Name</label><input id="p-name" value="${s.name}" maxlength="60" placeholder="Your name"></div><div class="field"><label>Level</label><select id="p-grade"><option ${s.grade==="School"?"selected":""}>School</option><option ${s.grade==="College"?"selected":""}>College</option><option ${s.grade==="University"?"selected":""}>University</option><option ${s.grade==="Professional"?"selected":""}>Professional</option></select></div><div class="field"><label>Learning goal</label><select id="p-goal"><option ${s.goal==="Build strong understanding"?"selected":""}>Build strong understanding</option><option ${s.goal==="Prepare for exams"?"selected":""}>Prepare for exams</option><option ${s.goal==="Fix weak concepts"?"selected":""}>Fix weak concepts</option><option ${s.goal==="Learn from fundamentals"?"selected":""}>Learn from fundamentals</option></select></div><div class="field"><label>Preferred answer mode</label><div class="choice-row">${["Text","Voice","Both"].map(x=>`<button data-pref="${x}" class="${s.preferred_input===x?"selected":""}">${x}</button>`).join("")}</div></div><div class="profile-save"><span id="profile-status">Profile changes are saved to your local learner database.</span><div class="profile-actions"><button class="ghost" id="logout">Log out</button><button class="primary" id="save-profile">Save profile →</button></div></div></section><div class="grid"><section class="panel"><small>WHAT PERSONALIZATION MEANS</small><h3>Profile + evidence</h3><p>LearnBack uses your level and goal to frame the experience, but it does not assume mastery from your profile. Concept mastery still comes from your actual learning signals.</p></section><section class="panel"><small>YOUR CURRENT FOCUS</small><h3>${s.goal}</h3><p>${s.attempts?`${s.attempts} learning signals have been collected so far.`:"Complete a first session to build your learning profile."}</p></section></div>`}

function classroom(){
  const highConf=s.history.filter(x=>x.misconception&&String(x.confidence).toLowerCase().includes("confident")).length;
  const wrong=s.history.filter(x=>!x.is_correct).length;
  const total=Math.max(42,Math.min(48,42+Math.floor(s.attempts/3)));
  const groups={foundation:Math.max(5,Math.min(12,8+Math.floor(wrong/3))),misconception:Math.max(4,Math.min(14,6+highConf)),developing:Math.max(9,Math.min(18,15+Math.floor(wrong/4))),advanced:Math.max(5,total-27)};
  const weak=Object.entries(s.mastery).sort((a,b)=>a[1]-b[1])[0];
  const weakLabel=weak?conceptLabel(weak[0]):"Foreign Key";
  const weakScore=weak?Math.round(weak[1]):54;
  return `${header("AI CLASSROOM COPILOT","Turn learning signals into teacher action","View knowledge map","classroom","progress")}
  <section class="classroom-hero"><div><small>TEACHER AI</small><h2>See what the class misunderstands — and decide what to do next.</h2><p>LearnBack aggregates student evidence into concept-level signals. AI suggests interventions; the teacher stays in control of the decision.</p><div class="hero-actions"><button class="primary" data-teacher-action="intervention">Create intervention →</button><button class="ghost" data-teacher-action="activity">Generate activity</button></div></div><div class="classroom-ring"><b>${Math.round((s.history.filter(x=>x.is_correct).length/Math.max(1,s.history.length))*100)||76}%</b><span>class understanding</span></div></section>
  <div class="stats classroom-stats"><div><small>STUDENTS</small><b>${total}</b><span>Active learners</span></div><div><small>CLASS MASTERY</small><b>76%</b><span>Concept estimate</span></div><div><small>NEEDS INTERVENTION</small><b>${groups.misconception}</b><span>Grouped by need</span></div><div><small>HIGH-CONFIDENCE ERRORS</small><b>${highConf||5}</b><span>Priority signals</span></div></div>
  <div class="classroom-grid"><section class="panel ai-recommend"><div class="head"><div><small>AI RECOMMENDATION</small><h3>What needs attention?</h3></div><span class="status-dot">LIVE</span></div><div class="recommend-card"><div class="recommend-icon">!</div><div><b>${weakLabel} is the current bottleneck</b><p>${highConf||5} students show evidence that needs targeted recovery. A short comparison activity followed by a re-test is recommended.</p><div class="recommend-actions"><button class="primary" data-teacher-action="intervention">Build 5-min intervention</button><button class="link" data-modal="progress">Inspect evidence</button></div></div><strong>${weakScore}%</strong></div></section>
  <section class="panel"><div class="head"><div><small>LEARNING GROUPS</small><h3>Act on needs, not marks</h3></div></div><div class="teacher-groups"><div><i>◉</i><div><b>Needs Foundation</b><span>${groups.foundation} students • core concepts</span></div><button data-teacher-action="foundation">Open</button></div><div><i>!</i><div><b>Misconception Recovery</b><span>${groups.misconception} students • targeted repair</span></div><button data-teacher-action="misconception">Open</button></div><div><i>↗</i><div><b>Developing</b><span>${groups.developing} students • guided practice</span></div><button data-teacher-action="developing">Open</button></div><div><i>★</i><div><b>Advanced</b><span>${groups.advanced} students • challenge path</span></div><button data-teacher-action="advanced">Open</button></div></div></section></div>
  <section class="panel intervention"><div class="head"><div><small>INTERVENTION OUTCOME</small><h3>Did teaching change understanding?</h3></div><span class="trend-note">AI measures before vs after</span></div><div class="before-after"><div><small>BEFORE</small><b>61%</b><em style="width:61%"></em></div><div class="arrow">→</div><div><small>AFTER</small><b>79%</b><em style="width:79%"></em></div><div class="gain">+18 pts</div></div></section>`;
}

function history(){let items="";if(s.history.length){items=s.history.map(x=>{const icon=x.misconception?"!":"✓";const state=x.is_correct?"Correct":"Needs work";const when=x.created_at?new Date(x.created_at).toLocaleString():"recent";return `<article><i>${icon}</i><div><b>${conceptLabel(x.concept)}</b><p>${x.summary}</p><small>${x.confidence} • ${state} • ${x.misconception_id||"No misconception"} • ${when}</small></div></article>`}).join("")}else items=`<div class="empty"><div>◎</div><h3>No learning signals yet</h3><p>Start a session and LearnBack will build the profile as you answer.</p><button class="primary" data-start="dbms">Start first session →</button></div>`;return `${header("LEARNING HISTORY","What LearnBack has learned","Overview","dashboard")}<section class="panel"><div class="head"><div><small>RECENT SIGNALS</small><h3>Analyzed answers</h3></div><button class="link" id="reset">Reset demo</button></div><div class="history">${items}</div></section>`}
async function start(topic:Topic,demo=false){s.modal=null;s.topic=topic;s.view="learn";s.q=null;s.answer="";s.confidence="";s.d=null;s.session=[];s.sessionStarted=Date.now();s.demoMode=demo;render();try{const r=await apiFetch(`${API}/api/next-question?topic=${topic}`);s.q=await readJson(r)}catch{s.q={type:"CONCEPT CHECK",question:topic==="dbms"?"What is the main purpose of a primary key in a database table?":topic==="java"?"What does encapsulation mean in object-oriented programming?":"What is a Python list and when would you use one?",hint:"Explain it in your own words.",focus:topic==="dbms"?"Primary Key":topic==="java"?"Encapsulation":"Lists",concept:topic==="dbms"?"primary_key":topic==="java"?"encapsulation":"lists",difficulty:1,reason:"Local demo starter question"}}render()}
function speak(){const SR:any=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SR)return;const rec=new SR();rec.lang="en-IN";rec.interimResults=false;rec.continuous=false;const btn=document.querySelector<HTMLButtonElement>("#voice");if(btn){btn.textContent="🎙 Listening…";btn.disabled=true}rec.onresult=(e:any)=>{s.answer=Array.from(e.results).map((r:any)=>r[0].transcript).join(" ");render()};rec.onerror=()=>render();rec.onend=()=>render();rec.start()}
async function submit(){s.loading=true;render();const q=s.q!;try{const r=await apiFetch(`${API}/api/analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({topic:s.topic,question:q.question,answer:s.answer,confidence:s.confidence,question_index:s.attempts,concept_hint:q.concept})});if(!r.ok){const data=await readJson(r);throw new Error((data && data.error) || "analysis failed")}const d=await readJson(r) as D;s.d=d;s.mastery[d.concept]=d.new_mastery??s.mastery[d.concept]??50;s.attempts++;s.session.push({correct:d.is_correct,confidence:s.confidence,misconception:d.misconception,concept:d.concept,label:d.concept_label});s.history.unshift({concept:d.concept,summary:d.summary,misconception:d.misconception,confidence:s.confidence,is_correct:d.is_correct,misconception_id:d.misconception_id,created_at:new Date().toISOString()})}catch{s.d={is_correct:false,concept:q.concept||"primary_key",concept_label:conceptLabel(q.concept||"primary_key"),misconception:false,misconception_id:null,confidence_assessment:"medium",summary:"The local learning engine is ready.",explanation:"The backend did not respond, so no answer was stored. Start npm run server and try again.",recovery:"Make sure the LearnBack backend is running on port 8787.",next_question:q.question,mastery_signal:0,mode:"setup"}}s.loading=false;render()}
async function continueSession(){if(s.session.length>=s.sessionTarget){s.d=null;s.view="summary";render();return}s.d=null;s.answer="";s.confidence="";s.q=null;render();try{const r=await apiFetch(`${API}/api/next-question?topic=${s.topic}`);s.q=await readJson(r)}catch{s.q={type:"TARGETED RETEST",question:s.topic==="dbms"?"Which column could uniquely identify every student in a Students table, and why?":s.topic==="java"?"Why might a Java class keep a field private and expose a public getter?":"When would you choose a Python list instead of a single variable?",hint:"Use a concrete example.",focus:s.topic==="dbms"?"Unique identification":s.topic==="java"?"Access control":"Lists",concept:s.topic==="dbms"?"primary_key":s.topic==="java"?"encapsulation":"lists",difficulty:2,reason:"Fallback targeted retest"}}render()}
async function reset(){if(!confirm("Reset the local LearnBack demo profile?"))return;try{await apiFetch(`${API}/api/reset`,{method:"POST"})}catch{}s.mastery={};s.attempts=0;s.history=[];s.session=[];s.modal=null;s.view="dashboard";s.demoMode=false;await boot(true)}

async function logout(){try{await apiFetch(`${API}/api/logout`,{method:"POST"})}catch{}localStorage.removeItem("learnback_session");s.authenticated=false;s.modal=null;s.view="dashboard";s.name="Student";s.mastery={};s.history=[];s.attempts=0;render()}
async function saveProfile(){const name=(document.querySelector<HTMLInputElement>("#p-name")?.value||"Student").trim();const grade=document.querySelector<HTMLSelectElement>("#p-grade")?.value||"College";const goal=document.querySelector<HTMLSelectElement>("#p-goal")?.value||"Build strong understanding";try{const r=await apiFetch(`${API}/api/profile`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,grade,goal,preferred_input:s.preferred_input})});const data=await readJson(r);s.name=data.user?.name||name;s.grade=data.user?.grade||grade;s.goal=data.user?.goal||goal;s.preferred_input=data.user?.preferred_input||s.preferred_input;s.modal=null;s.view="dashboard";render()}catch{const el=document.querySelector("#profile-status");if(el)el.textContent="Could not save. Make sure the backend is running."}}

function subjects(){const cards=[{id:"dbms",code:"DB",name:"Database Systems",desc:"Keys, relationships, normalization and misconceptions.",chapters:["Database Keys","Foreign Keys","Normalization"]},{id:"java",code:"JV",name:"Java OOP",desc:"Build object-oriented thinking from fundamentals to transfer.",chapters:["Encapsulation","Inheritance","Polymorphism"]},{id:"python",code:"PY",name:"Python Foundations",desc:"Core Python concepts with adaptive checks and recovery.",chapters:["Lists","Functions","Exceptions"]}];return `${header("LEARNING LIBRARY","Choose what you want to understand","Close","dashboard")}<div class="subject-grid">${cards.map(c=>`<article class="subject-card"><div class="subject-icon">${c.code}</div><small>SUBJECT</small><h3>${c.name}</h3><p>${c.desc}</p><div class="chapter-list">${c.chapters.map((ch,i)=>`<span><b>${i+1}</b>${ch}</span>`).join("")}</div><button class="primary" data-start="${c.id}">Start adaptive path →</button></article>`).join("")}</div><section class="library-note"><b>How LearnBack chooses the next question</b><span>It looks at concept mastery, recent mistakes, confidence and prerequisite relationships instead of following a fixed quiz order.</span></section>`}
function modalContent(v:View){if(v==="classroom")return classroom();if(v==="progress")return progress();if(v==="plan")return plan();if(v==="history")return history();if(v==="profile")return profile();if(v==="subjects")return subjects();return dashboard()}
function modal(){if(!s.modal)return "";const title=s.modal==="classroom"?"AI Classroom Copilot":s.modal==="progress"?"Knowledge map":s.modal==="plan"?"Learning plan":s.modal==="history"?"History":s.modal==="profile"?"My profile":s.modal==="subjects"?"Subjects":"Overview";return `<div class="modal-overlay" id="modal-overlay"><div class="modal-sheet" role="dialog" aria-modal="true" aria-label="${title}"><div class="modal-bar"><div><span class="modal-dot"></span><b>${title}</b></div><button class="modal-close" id="modal-close" aria-label="Close">×</button></div><div class="modal-content">${modalContent(s.modal)}</div></div></div>`}

function bind(){
  document.querySelectorAll<HTMLElement>("[data-auth-switch]").forEach(x=>x.onclick=()=>{s.authMode=x.dataset.authSwitch as "login"|"signup";s.authError="";render()});
  document.querySelector("#auth-submit")?.addEventListener("click",authSubmit);
  document.querySelector<HTMLInputElement>("#auth-password")?.addEventListener("keydown",e=>{if(e.key==="Enter")authSubmit()});
  document.querySelectorAll<HTMLElement>("[data-modal]").forEach(x=>x.onclick=()=>{const target=x.dataset.modal as View;if(target==="dashboard"){s.modal=null;s.view="dashboard";render();return}s.modal=target;render()});
  document.querySelectorAll<HTMLElement>("[data-v]").forEach(x=>x.onclick=()=>{const target=x.dataset.v as View;if(target==="learn"){s.modal=null;start(s.topic);return}s.modal=null;s.view=target;render()});
  document.querySelector("#modal-close")?.addEventListener("click",()=>{s.modal=null;render()});
  document.querySelector("#modal-overlay")?.addEventListener("click",e=>{if(e.target===e.currentTarget){s.modal=null;render()}});
  document.querySelectorAll<HTMLElement>("[data-start]").forEach(x=>x.onclick=()=>start(x.dataset.start as Topic));
  document.querySelectorAll<HTMLElement>("[data-teacher-action]").forEach(x=>x.onclick=()=>{const action=String(x.dataset.teacherAction||"");if(action==="intervention"||action==="activity"){s.modal="classroom";render();setTimeout(()=>toast(action==="intervention"?"Intervention builder opened — review the AI recommendation before assigning.":"Activity generator opened — review and customize the generated activity."),0);return}toast(`Teacher group selected: ${action}.`)});
  document.querySelectorAll<HTMLElement>("[data-demo]").forEach(x=>x.onclick=()=>start("dbms",true));
  document.querySelector("[data-fill-demo]")?.addEventListener("click",()=>{s.answer="It connects two tables, like a link between records.";s.confidence="Very confident";render();toast("Sample misconception loaded — analyze it to see LearnBack adapt.")});
  document.querySelectorAll<HTMLElement>("[data-c]").forEach(x=>x.onclick=()=>{s.confidence=x.dataset.c!;render()});
  document.querySelector<HTMLTextAreaElement>("#answer")?.addEventListener("input",e=>{s.answer=(e.target as HTMLTextAreaElement).value});
  document.querySelector("#submit")?.addEventListener("click",submit);
  document.querySelector("#continue")?.addEventListener("click",continueSession);
  document.querySelector("#voice")?.addEventListener("click",speak);
  document.querySelector("#reset")?.addEventListener("click",reset);
  document.querySelectorAll<HTMLElement>("[data-pref]").forEach(x=>x.onclick=()=>{s.preferred_input=x.dataset.pref!;render()});
  document.querySelector("#save-profile")?.addEventListener("click",saveProfile);
  document.querySelector("#logout")?.addEventListener("click",logout);
}
function toast(message:string){s.toast=message;render();window.setTimeout(()=>{if(s.toast===message){s.toast="";render()}},3200)}
document.addEventListener("keydown",e=>{if(e.key==="Escape"&&s.modal){s.modal=null;render()}});
function render(){if(s.booting){app.innerHTML=`<div class="boot"><div class="logo big">L</div><h2>Loading your learning profile…</h2></div>`;return}if(!s.authenticated){app.innerHTML=authScreen();bind();return}app.innerHTML=`<div class="shell">${side()}<main>${s.view==="dashboard"?dashboard():s.view==="learn"?learn():s.view==="progress"?progress():s.view==="summary"?summary():s.view==="plan"?plan():s.view==="profile"?profile():history()}</main></div>${modal()}${s.toast?`<div class="toast" role="status"><span>✦</span>${s.toast}</div>`:""}`;bind()}
async function boot(reload=false){s.booting=!reload;render();try{const pr=await apiFetch(`${API}/api/profile`);if(pr.status===401){s.authenticated=false;s.booting=false;render();return}const p=await readJson(pr);s.authenticated=true;s.name=p.user?.name||"Student";s.grade=p.user?.grade||"College";s.goal=p.user?.goal||"Build strong understanding";s.preferred_input=p.user?.preferred_input||"Text";s.attempts=Number(p.attempts||0);s.mastery=p.mastery||{};s.map=p.concepts||[];const hResponse=await apiFetch(`${API}/api/history`);const h=await readJson(hResponse);s.history=h.items||[]}catch{}s.booting=false;render()}
boot();
